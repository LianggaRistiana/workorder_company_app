import 'package:flutter/material.dart';

class ServiceUpdatePage extends StatelessWidget {
  const ServiceUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}