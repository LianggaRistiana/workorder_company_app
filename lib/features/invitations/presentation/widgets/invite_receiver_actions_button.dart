import 'package:flutter/material.dart';

// TODO : Receiver (staff unssigned) actions button ex: accept, reject 
class InviteReceiverActionsButton extends StatelessWidget {
  const InviteReceiverActionsButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
