import 'package:flutter/material.dart';

// TODO : Owner and manager actions button ex: cancel
class InviteSenderActionsButton extends StatelessWidget {
  const InviteSenderActionsButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}